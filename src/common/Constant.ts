const VERSION = '0.1.1';
const VERSION_STRING = `Ver. ${VERSION}(2024. 07. 19)`;

const DEFAULT_USER = {
  userId: 'boksl',
  passwd: 'boksl',
  name: '복슬이',
};

const Constant = {
  VERSION,
  VERSION_STRING,
  DEFAULT_USER,
};

export default Constant;
