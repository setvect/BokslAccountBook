const VERSION = '0.0.1';
const VERSION_STRING = `Ver. ${VERSION}(2023. 12. 21)`;
const DB_PATH = 'db/BokslAccountBook.db';

const DEFAULT_USER = {
  userId: 'boksl',
  passwd: 'boksl',
  name: '복슬이',
};

const Constant = {
  VERSION,
  VERSION_STRING,
  DEFAULT_USER,
  DB_PATH,
};

export default Constant;
